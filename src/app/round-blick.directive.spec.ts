import { RoundBlickDirective } from './round-blick.directive';

describe('RoundBlickDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBlickDirective();
    expect(directive).toBeTruthy();
  });
});
