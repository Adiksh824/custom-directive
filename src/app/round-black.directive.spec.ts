import { RoundBlackDirective } from './round-black.directive';

describe('RoundBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundBlackDirective();
    expect(directive).toBeTruthy();
  });
});
